[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && npm run build && ls dist/"

[deploy]
startCommand = "node dist/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
